import type { backendInterface } from "../backend";
import { AssetId, NnsProposalStatus, Variant_bear_bull_neutral, Variant_other_mission70_governance_chainFusion } from "../backend";

const mockSnapshot = {
  cycleBurnRateTcycles: 12.4,
  icpPrice24hChange: 3.7,
  icpPriceUsd: 11.28,
  devCommitsWeekly: BigInt(842),
  sentimentScore: 0.72,
  totalCanistersDeployed: BigInt(183400),
  tvlUsd: 234000000,
  timestamp: BigInt(Date.now()),
  activeAddresses7d: BigInt(48200),
  activeAddresses24h: BigInt(9300),
  activeAddresses30d: BigInt(120000),
  burnRateIsReal: false,
  activeAddressesIsReal: false,
};

const mockSnapshot2 = {
  cycleBurnRateTcycles: 11.8,
  icpPrice24hChange: -1.2,
  icpPriceUsd: 10.85,
  devCommitsWeekly: BigInt(780),
  sentimentScore: 0.55,
  totalCanistersDeployed: BigInt(182000),
  tvlUsd: 220000000,
  timestamp: BigInt(Date.now() - 86400000),
  activeAddresses7d: BigInt(46000),
  activeAddresses24h: BigInt(8700),
  activeAddresses30d: BigInt(118000),
  burnRateIsReal: false,
  activeAddressesIsReal: false,
};

const mockDataSourceStatus = {
  icpPriceLive: true,
  burnRateLive: false,
  activeAddressesLive: false,
  proposalsLive: false,
};

const mockProposals = [
  {
    id: BigInt(131000),
    title: "Mission 70 Milestone: First 10 SNS DAOs Graduated to Full Autonomy",
    summary: "Verifies that 10 SNS-governed DAOs have completed the graduation process and no longer rely on developer neurons.",
    status: NnsProposalStatus.open,
    topic: "ecosystem growth",
    proposer: "Neuron 12345",
    votesYes: BigInt(8_000_000),
    votesNo: BigInt(500_000),
    votingPeriodEnd: BigInt(Date.now() + 7 * 86400 * 1000),
    isMission70Related: true,
  },
  {
    id: BigInt(130998),
    title: "Mission 70 Milestone: Chain Fusion BTC Integration Audit Complete",
    summary: "Confirms third-party security audit of the Bitcoin Chain Fusion integration has passed all critical checks.",
    status: NnsProposalStatus.adopted,
    topic: "chain fusion",
    proposer: "Neuron 99001",
    votesYes: BigInt(14_500_000),
    votesNo: BigInt(280_000),
    votingPeriodEnd: BigInt(Date.now() - 2 * 86400 * 1000),
    isMission70Related: true,
  },
  {
    id: BigInt(130995),
    title: "Mission 70 Milestone: 500 Active Developer Teams on ICP",
    summary: "Tracks the growth target of 500 independently operating developer teams deploying production canisters on ICP.",
    status: NnsProposalStatus.executing,
    topic: "ecosystem growth",
    proposer: "Neuron 55678",
    votesYes: BigInt(11_200_000),
    votesNo: BigInt(620_000),
    votingPeriodEnd: BigInt(Date.now() - 1 * 86400 * 1000),
    isMission70Related: true,
  },
  {
    id: BigInt(130999),
    title: "Increase NNS Proposal Voting Period to 4 Days",
    summary: "Extends the default voting period for governance proposals from 3 to 4 days to allow more neuron participation.",
    status: NnsProposalStatus.open,
    topic: "governance",
    proposer: "Neuron 67890",
    votesYes: BigInt(4_200_000),
    votesNo: BigInt(1_100_000),
    votingPeriodEnd: BigInt(Date.now() + 3 * 86400 * 1000),
    isMission70Related: false,
  },
  {
    id: BigInt(130990),
    title: "Enable HTTP Outcalls for All Subnet Types",
    summary: "Removes subnet-type restrictions on HTTP outcall functionality, enabling all canisters to make external API calls.",
    status: NnsProposalStatus.adopted,
    topic: "protocol",
    proposer: "Neuron 34112",
    votesYes: BigInt(18_300_000),
    votesNo: BigInt(150_000),
    votingPeriodEnd: BigInt(Date.now() - 5 * 86400 * 1000),
    isMission70Related: false,
  },
  {
    id: BigInt(130985),
    title: "Reduce Minimum Neuron Stake to 0.5 ICP",
    summary: "Lowers the barrier to participate in NNS governance by reducing the minimum ICP required to create a neuron.",
    status: NnsProposalStatus.rejected,
    topic: "governance",
    proposer: "Neuron 20055",
    votesYes: BigInt(3_100_000),
    votesNo: BigInt(9_800_000),
    votingPeriodEnd: BigInt(Date.now() - 10 * 86400 * 1000),
    isMission70Related: false,
  },
  {
    id: BigInt(130980),
    title: "Deploy Upgraded Consensus Protocol v3.2",
    summary: "Rolls out consensus protocol improvements targeting 15% latency reduction across all subnet types.",
    status: NnsProposalStatus.executed,
    topic: "protocol",
    proposer: "Neuron 10001",
    votesYes: BigInt(22_000_000),
    votesNo: BigInt(80_000),
    votingPeriodEnd: BigInt(Date.now() - 14 * 86400 * 1000),
    isMission70Related: false,
  },
];

export const mockBackend: backendInterface = {
  getLatestSnapshot: async () => mockSnapshot,
  getLatestSnapshotByAsset: async (assetId: string) => ({
    assetId: assetId as AssetId,
    price: assetId === "BTC" ? 67000 : assetId === "ETH" ? 3500 : assetId === "SOL" ? 180 : assetId === "XRP" ? 0.62 : assetId === "ADA" ? 0.48 : assetId === "AVAX" ? 38 : assetId === "INJ" ? 28 : 1.85,
    priceChange24h: (Math.random() - 0.5) * 10,
    priceChange7d: (Math.random() - 0.5) * 20,
    marketCap: assetId === "BTC" ? 1_300_000_000_000 : assetId === "ETH" ? 420_000_000_000 : 50_000_000_000,
    volume24h: assetId === "BTC" ? 35_000_000_000 : 5_000_000_000,
    sentimentScore: BigInt(Math.floor(50 + (Math.random() - 0.5) * 40)),
    metrics: [["staking_ratio", 0.65], ["active_validators", 1200]],
    events: [],
    timestamp: BigInt(Date.now()),
  }),
  getSnapshotHistoryByAsset: async (assetId: string, _limit: bigint) => {
    const base = assetId === "BTC" ? 67000 : assetId === "ETH" ? 3500 : 180;
    return Array.from({ length: 7 }, (_, i) => ({
      assetId: assetId as AssetId,
      price: base + (Math.sin(i * 0.5) * base * 0.05),
      priceChange24h: (Math.random() - 0.5) * 10,
      priceChange7d: (Math.random() - 0.5) * 20,
      marketCap: base * 1_000_000,
      volume24h: base * 50_000,
      sentimentScore: BigInt(Math.floor(55 + i * 2)),
      metrics: [],
      events: [],
      timestamp: BigInt(Date.now() - (6 - i) * 86_400_000),
    }));
  },
  getGovernanceProposalsByAsset: async (_assetId: string) => [],
  refreshAsset: async (_assetIdText: string) => {},
  refreshAllAssets: async () => {},
  getProtocolEvents: async () => [
    {
      title: "Mission 70 Launch — ICP sets goal of 70 sovereign projects",
      date: "2024-04-01",
      category: Variant_other_mission70_governance_chainFusion.mission70,
    },
    {
      title: "Chain Fusion ETH Integration — native Ethereum connectivity live",
      date: "2025-01-15",
      category: Variant_other_mission70_governance_chainFusion.chainFusion,
    },
    {
      title: "SNS Governance Expansion — new framework for community-run DAOs",
      date: "2024-11-10",
      category: Variant_other_mission70_governance_chainFusion.governance,
    },
    {
      title: "ICP Ecosystem Fund Announcement — $50M allocated for developer grants",
      date: "2025-02-20",
      category: Variant_other_mission70_governance_chainFusion.other,
    },
    {
      title: "Community-Driven Neuron Initiative — grassroots governance participation surge",
      date: "2025-03-05",
      category: Variant_other_mission70_governance_chainFusion.governance,
    },
  ],
  getSentimentBreakdown: async () => ({
    overallScore: 0.72,
    drivingFactors: [
      { name: "ICP Price", value: "$11.28", signal: Variant_bear_bull_neutral.bull },
      { name: "TVL", value: "$234M", signal: Variant_bear_bull_neutral.bull },
      { name: "Active Addresses 24h", value: "9,300", signal: Variant_bear_bull_neutral.neutral },
      { name: "Dev Commits", value: "842", signal: Variant_bear_bull_neutral.bull },
      { name: "Cycle Burn Rate", value: "12.4T", signal: Variant_bear_bull_neutral.neutral },
      { name: "Canisters Deployed", value: "183,400", signal: Variant_bear_bull_neutral.bull },
    ],
  }),
  getSnapshotHistory: async (_days: bigint) => [mockSnapshot2, mockSnapshot],
  refreshMetrics: async () => ({
    __kind__: "ok",
    ok: { snapshot: mockSnapshot, dataSourceStatus: mockDataSourceStatus },
  }),
  getGovernanceProposals: async () => mockProposals,
  getOpenProposals: async () => mockProposals.filter((p) => p.status === NnsProposalStatus.open),
  getRecentProposals: async (limit: bigint) => mockProposals.slice(0, Number(limit)),
  transform: async (input) => ({
    status: BigInt(200),
    body: input.response.body,
    headers: input.response.headers,
  }),
  transform_burnrate: async (input) => ({
    status: BigInt(200),
    body: input.response.body,
    headers: input.response.headers,
  }),
  transform_addresses: async (input) => ({
    status: BigInt(200),
    body: input.response.body,
    headers: input.response.headers,
  }),
  transform_cg_chart: async (input) => ({
    status: BigInt(200),
    body: input.response.body,
    headers: input.response.headers,
  }),
  transform_cg_markets: async (input) => ({
    status: BigInt(200),
    body: input.response.body,
    headers: input.response.headers,
  }),
  transform_proposals: async (input) => ({
    status: BigInt(200),
    body: input.response.body,
    headers: input.response.headers,
  }),
};
